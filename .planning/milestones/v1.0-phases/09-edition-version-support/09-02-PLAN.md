---
phase: 09-edition-version-support
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python/src/odoo_gen_utils/templates/17.0/view_form.xml.j2
  - python/src/odoo_gen_utils/templates/17.0/view_tree.xml.j2
  - python/src/odoo_gen_utils/templates/17.0/action.xml.j2
  - python/src/odoo_gen_utils/templates/17.0/model.py.j2
  - python/src/odoo_gen_utils/templates/18.0/view_form.xml.j2
  - python/src/odoo_gen_utils/templates/18.0/view_tree.xml.j2
  - python/src/odoo_gen_utils/templates/18.0/action.xml.j2
  - python/src/odoo_gen_utils/templates/18.0/model.py.j2
  - python/src/odoo_gen_utils/templates/shared/access_csv.j2
  - python/src/odoo_gen_utils/templates/shared/init_models.py.j2
  - python/src/odoo_gen_utils/templates/shared/init_root.py.j2
  - python/src/odoo_gen_utils/templates/shared/init_tests.py.j2
  - python/src/odoo_gen_utils/templates/shared/init_wizards.py.j2
  - python/src/odoo_gen_utils/templates/shared/menu.xml.j2
  - python/src/odoo_gen_utils/templates/shared/readme.rst.j2
  - python/src/odoo_gen_utils/templates/shared/security_group.xml.j2
  - python/src/odoo_gen_utils/templates/shared/record_rules.xml.j2
  - python/src/odoo_gen_utils/templates/shared/sequences.xml.j2
  - python/src/odoo_gen_utils/templates/shared/demo_data.xml.j2
  - python/src/odoo_gen_utils/templates/shared/wizard.py.j2
  - python/src/odoo_gen_utils/templates/shared/wizard_form.xml.j2
  - python/src/odoo_gen_utils/templates/shared/test_model.py.j2
  - python/src/odoo_gen_utils/templates/shared/view_search.xml.j2
  - python/src/odoo_gen_utils/templates/shared/manifest.py.j2
  - python/src/odoo_gen_utils/renderer.py
  - python/tests/test_renderer.py
autonomous: true
requirements: [VERS-04, VERS-05, VERS-06]

must_haves:
  truths:
    - "render_module() with odoo_version=17.0 produces views using <tree> tag and view_mode=tree,form"
    - "render_module() with odoo_version=18.0 produces views using <list> tag and view_mode=list,form"
    - "18.0 form view uses <chatter/> shorthand instead of <div class=oe_chatter>"
    - "Version-specific templates are selected from 17.0/ or 18.0/ directory, shared templates from shared/"
    - "Existing 17.0 generation is not broken (backward compatible)"
    - "odoo_version from spec flows through to create_versioned_renderer()"
  artifacts:
    - path: "python/src/odoo_gen_utils/templates/17.0/"
      provides: "Version-specific templates for Odoo 17.0 (tree tag, verbose chatter)"
    - path: "python/src/odoo_gen_utils/templates/18.0/"
      provides: "Version-specific templates for Odoo 18.0 (list tag, chatter shorthand)"
    - path: "python/src/odoo_gen_utils/templates/shared/"
      provides: "Version-independent templates moved from flat directory"
    - path: "python/src/odoo_gen_utils/renderer.py"
      provides: "create_versioned_renderer() with FileSystemLoader fallback chain"
      exports: ["create_versioned_renderer", "create_renderer", "render_module"]
  key_links:
    - from: "python/src/odoo_gen_utils/renderer.py"
      to: "python/src/odoo_gen_utils/templates/18.0/"
      via: "FileSystemLoader([version_dir, shared_dir])"
      pattern: "FileSystemLoader"
    - from: "python/src/odoo_gen_utils/renderer.py:render_module"
      to: "spec['odoo_version']"
      via: "spec.get('odoo_version', '17.0')"
      pattern: "spec\\.get.*odoo_version"
---

<objective>
Reorganize templates into versioned directories (17.0/, 18.0/, shared/) and make the renderer version-aware with FileSystemLoader fallback chain.

Purpose: VERS-04/05/06 -- enable generating modules targeting either Odoo 17.0 or 18.0 with correct version-specific syntax (tree vs list, verbose vs shorthand chatter, view_mode differences). The renderer reads odoo_version from the spec and selects the right template set.

Output: Reorganized template dirs, versioned renderer, updated tests
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-edition-version-support/09-CONTEXT.md
@.planning/phases/09-edition-version-support/09-RESEARCH.md

<interfaces>
<!-- Current renderer API that must be preserved for backward compatibility -->

From python/src/odoo_gen_utils/renderer.py:
```python
def create_renderer(template_dir: Path) -> Environment:
    """Existing function -- KEEP for backward compatibility."""

def get_template_dir() -> Path:
    """Returns Path(__file__).parent / 'templates' -- still the entry point."""

def render_module(spec: dict[str, Any], template_dir: Path, output_dir: Path) -> list[Path]:
    """Currently calls create_renderer(template_dir). Must be updated to call
    create_versioned_renderer() using spec.get('odoo_version', '17.0')."""
```

From python/src/odoo_gen_utils/cli.py (DO NOT MODIFY -- cli.py changes in Plan 03):
```python
# render_module_cmd calls render_module(spec, template_dir, output_path)
# render cmd calls create_renderer(template_dir) then render_template()
# Both must continue to work with backward-compatible API
```

Current templates (flat directory -- to be reorganized):
```
templates/
  access_csv.j2, action.xml.j2, demo_data.xml.j2, init_models.py.j2,
  init_root.py.j2, init_tests.py.j2, init_wizards.py.j2, manifest.py.j2,
  menu.xml.j2, model.py.j2, readme.rst.j2, record_rules.xml.j2,
  security_group.xml.j2, sequences.xml.j2, test_model.py.j2,
  view_form.xml.j2, view_search.xml.j2, view_tree.xml.j2,
  wizard_form.xml.j2, wizard.py.j2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize templates into 17.0/, 18.0/, shared/ directories</name>
  <files>
    python/src/odoo_gen_utils/templates/17.0/view_form.xml.j2,
    python/src/odoo_gen_utils/templates/17.0/view_tree.xml.j2,
    python/src/odoo_gen_utils/templates/17.0/action.xml.j2,
    python/src/odoo_gen_utils/templates/17.0/model.py.j2,
    python/src/odoo_gen_utils/templates/18.0/view_form.xml.j2,
    python/src/odoo_gen_utils/templates/18.0/view_tree.xml.j2,
    python/src/odoo_gen_utils/templates/18.0/action.xml.j2,
    python/src/odoo_gen_utils/templates/18.0/model.py.j2,
    python/src/odoo_gen_utils/templates/shared/
  </files>
  <action>
    **Step 1: Create directory structure**
    ```
    templates/17.0/
    templates/18.0/
    templates/shared/
    ```

    **Step 2: Move shared templates**

    COPY (not move yet -- keep originals until renderer is updated) these templates to `templates/shared/`:
    access_csv.j2, init_models.py.j2, init_root.py.j2, init_tests.py.j2,
    init_wizards.py.j2, menu.xml.j2, readme.rst.j2, security_group.xml.j2,
    record_rules.xml.j2, sequences.xml.j2, demo_data.xml.j2, wizard.py.j2,
    wizard_form.xml.j2, test_model.py.j2, view_search.xml.j2, manifest.py.j2

    **Step 3: Copy version-specific templates to 17.0/**

    Copy these to `templates/17.0/` (they are the CURRENT 17.0-correct versions):
    view_form.xml.j2, view_tree.xml.j2, action.xml.j2, model.py.j2

    **Step 4: Create 18.0 variants**

    Create `templates/18.0/` versions with these specific changes:

    `view_form.xml.j2` (18.0):
    - All `<tree` tags become `<list` tags, `</tree>` becomes `</list>`
    - Chatter section changes from `<div class="oe_chatter">...<field name="message_follower_ids"/>...<field name="message_ids"/>...</div>` to just `<chatter/>`
    - Keep everything else identical

    `view_tree.xml.j2` (18.0):
    - `<tree` becomes `<list`, `</tree>` becomes `</list>`
    - view name: `.view.list` instead of `.view.tree`

    `action.xml.j2` (18.0):
    - `view_mode` value: `list,form` instead of `tree,form`

    `model.py.j2` (18.0):
    - If template uses `group_operator=`, change to `aggregator=`
    - If template uses `states=` parameter on fields, remove it
    - Note: current 17.0 templates likely don't use these, but create the 18.0 variant as a copy with a comment header noting it's the 18.0 version. This ensures future additions are version-correct.

    **Step 5: Delete original flat templates**

    After Step 4 is complete, remove the original flat template files from `templates/` root. The files are now in `17.0/`, `18.0/`, and `shared/`.

    **CRITICAL anti-pattern:** Do NOT use `{% if odoo_version == '18.0' %}` inside any template. Separate files per version.

    **IMPORTANT:** Do NOT modify cli.py. CLI wiring happens in Plan 03.
  </action>
  <verify>
    <automated>ls /home/inshal-rauf/Odoo_module_automation/python/src/odoo_gen_utils/templates/17.0/ /home/inshal-rauf/Odoo_module_automation/python/src/odoo_gen_utils/templates/18.0/ /home/inshal-rauf/Odoo_module_automation/python/src/odoo_gen_utils/templates/shared/ && echo "All directories exist"</automated>
  </verify>
  <done>
    - templates/17.0/ has 4 version-specific templates (view_form, view_tree, action, model)
    - templates/18.0/ has 4 version-specific templates with list tag, list view_mode, chatter shorthand
    - templates/shared/ has 16 version-independent templates
    - Original flat .j2 files removed from templates/ root
    - 18.0 view_form uses list not tree, chatter/ not div.oe_chatter
    - 18.0 action uses view_mode="list,form"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Version-aware renderer with FileSystemLoader fallback + tests</name>
  <files>
    python/src/odoo_gen_utils/renderer.py,
    python/tests/test_renderer.py
  </files>
  <behavior>
    - TestVersionedTemplates::test_17_gets_tree_tag: render_module with odoo_version=17.0 produces XML containing "<tree" (not "<list")
    - TestVersionedTemplates::test_18_gets_list_tag: render_module with odoo_version=18.0 produces XML containing "<list" (not "<tree")
    - TestVersionedTemplates::test_18_action_uses_list_viewmode: 18.0 action.xml contains view_mode with "list,form"
    - TestVersionedTemplates::test_17_action_uses_tree_viewmode: 17.0 action.xml contains view_mode with "tree,form"
    - TestVersionedTemplates::test_18_chatter_shorthand: 18.0 form view uses "<chatter/>" not "oe_chatter"
    - TestVersionedTemplates::test_shared_template_fallback: Shared templates (manifest, menu, etc.) resolve correctly for both versions
    - TestVersionConfig::test_default_version_is_17: render_module with no odoo_version in spec defaults to 17.0
    - TestVersionConfig::test_version_from_spec: render_module reads odoo_version from spec dict
    - TestRenderModule18::test_full_18_module_renders: Complete render_module with odoo_version=18.0 produces all expected files without errors
  </behavior>
  <action>
    **Step 1: Write tests first (RED)**

    Add new test classes to `python/tests/test_renderer.py`:
    - `TestVersionedTemplates` -- 6 tests checking version-specific output content
    - `TestVersionConfig` -- 2 tests checking version flows through spec
    - `TestRenderModule18` -- 1 integration test rendering a full 18.0 module

    Each test uses `render_module()` with a spec containing `"odoo_version": "17.0"` or `"18.0"`, renders to a tmp dir, and checks the output file contents for version-specific markers (tree vs list, chatter syntax, view_mode).

    Run tests -- they must FAIL (renderer still uses flat directory).

    **Step 2: Update renderer.py (GREEN)**

    Add `create_versioned_renderer(version: str) -> Environment`:
    ```python
    def create_versioned_renderer(version: str) -> Environment:
        base = Path(__file__).parent / "templates"
        version_dir = str(base / version)
        shared_dir = str(base / "shared")
        loader = FileSystemLoader([version_dir, shared_dir])
        env = Environment(
            loader=loader,
            undefined=StrictUndefined,
            keep_trailing_newline=True,
            trim_blocks=True,
            lstrip_blocks=True,
        )
        env.filters["model_ref"] = _model_ref
        env.filters["to_class"] = _to_class
        env.filters["to_python_var"] = _to_python_var
        env.filters["to_xml_id"] = _to_xml_id
        return env
    ```

    Update `render_module()`:
    - Read version: `version = spec.get("odoo_version", "17.0")`
    - Replace `env = create_renderer(template_dir)` with `env = create_versioned_renderer(version)`
    - The `template_dir` parameter is still accepted for backward compatibility but is no longer used when `odoo_version` is in the spec. Add a note in the docstring.

    Update `get_template_dir()` to return the base templates dir (unchanged -- still `Path(__file__).parent / "templates"`).

    Keep `create_renderer()` unchanged for backward compatibility (the `render` CLI command and other callers still use it). But note: `create_renderer` with the old flat path will fail now since templates are moved. Update it to default to `create_versioned_renderer("17.0")` when called with the base templates dir.

    **Step 3: Run tests (GREEN)**

    All new tests + all existing tests must pass. The key is backward compatibility:
    - Existing tests that use `render_module()` without `odoo_version` in spec get 17.0 (default)
    - The `create_renderer(get_template_dir())` path still works by detecting the base dir and falling back to versioned loading

    **IMPORTANT:** Do NOT modify cli.py. CLI wiring happens in Plan 03.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_renderer.py -x -v</automated>
  </verify>
  <done>
    - create_versioned_renderer() exists and uses FileSystemLoader([version_dir, shared_dir])
    - render_module() reads odoo_version from spec (default "17.0")
    - 17.0 output has tree tags, 18.0 output has list tags
    - 18.0 output has chatter/ shorthand
    - All existing renderer tests still pass (backward compatible)
    - Full module renders without error for both 17.0 and 18.0
  </done>
</task>

</tasks>

<verification>
cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_renderer.py -x -v
cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/ -x -q
</verification>

<success_criteria>
- Templates organized in 17.0/, 18.0/, shared/ directories
- render_module(spec_with_17) produces tree-based output
- render_module(spec_with_18) produces list-based output
- No regression in existing tests
- create_versioned_renderer() exported from renderer.py
</success_criteria>

<output>
After completion, create `.planning/phases/09-edition-version-support/09-02-SUMMARY.md`
</output>
