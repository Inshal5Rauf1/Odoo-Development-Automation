---
phase: 15-odoo-dev-instance
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - python/tests/test_dev_instance.py
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "Unit tests for management script logic pass without Docker"
    - "Integration tests verify compose up, XML-RPC auth, module installation, and persistence when Docker is available"
    - "Tests are properly marked with docker marker and skipped when Docker is unavailable"
    - "All existing tests still pass (no regressions)"
  artifacts:
    - path: "python/tests/test_dev_instance.py"
      provides: "Unit + integration tests for Phase 15 dev instance"
      min_lines: 80
  key_links:
    - from: "python/tests/test_dev_instance.py"
      to: "scripts/odoo-dev.sh"
      via: "subprocess.run calls to execute the management script"
      pattern: "subprocess\\.run.*odoo-dev\\.sh"
    - from: "python/tests/test_dev_instance.py"
      to: "scripts/verify-odoo-dev.py"
      via: "imports or subprocess call to verify_xmlrpc function"
      pattern: "verify_xmlrpc|verify-odoo-dev"
    - from: "python/tests/test_dev_instance.py"
      to: "docker/dev/docker-compose.yml"
      via: "docker compose -f path for integration tests"
      pattern: "docker/dev/docker-compose\\.yml"
---

<objective>
Create comprehensive tests for the Phase 15 dev instance infrastructure.

Purpose: Verify that Docker Compose config, management script, and XML-RPC connectivity work correctly. Unit tests validate script logic without Docker; integration tests (docker-marked) validate the full stack when Docker is available.

Output: Test file covering all MCP-01 acceptance criteria.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-odoo-dev-instance/15-RESEARCH.md
@.planning/phases/15-odoo-dev-instance/15-01-SUMMARY.md

@docker/dev/docker-compose.yml
@docker/dev/odoo.conf
@scripts/odoo-dev.sh
@scripts/verify-odoo-dev.py
@python/pyproject.toml

<interfaces>
<!-- From existing project test conventions -->

pytest markers in pyproject.toml:
- `docker`: Integration tests requiring Docker daemon. Skipped when Docker is unavailable.
- `e2e`: End-to-end tests hitting real external services.

Docker availability check pattern (from test_docker_integration.py):
```python
from odoo_gen_utils.validation.docker_runner import check_docker_available
skip_no_docker = pytest.mark.skipif(
    not check_docker_available(), reason="Docker not available"
)
```

Existing compose file reference: docker/docker-compose.yml (ephemeral, tmpfs)
New compose file: docker/dev/docker-compose.yml (persistent, named volumes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit tests for dev instance config and script validation</name>
  <files>python/tests/test_dev_instance.py</files>
  <behavior>
    - test_compose_file_exists: docker/dev/docker-compose.yml exists
    - test_compose_uses_named_volumes: compose file contains "odoo-dev-db" and "odoo-dev-data" (not tmpfs)
    - test_compose_separate_from_validation: dev compose has `name: odoo-dev`, different from validation compose
    - test_compose_healthcheck_uses_python: healthcheck uses python3 urllib (not curl)
    - test_odoo_conf_db_name: docker/dev/odoo.conf contains db_name = odoo_dev
    - test_odoo_conf_list_db_false: docker/dev/odoo.conf contains list_db = False
    - test_env_file_defaults: docker/dev/.env contains ODOO_DEV_PORT=8069
    - test_management_script_executable: scripts/odoo-dev.sh has executable permission
    - test_management_script_uses_run_not_exec_for_init: scripts/odoo-dev.sh _init_modules uses "run --rm" (not "exec")
    - test_management_script_has_all_commands: scripts/odoo-dev.sh contains start|stop|status|reset|logs
    - test_verify_script_parseable: scripts/verify-odoo-dev.py parses as valid Python AST
    - test_verify_script_checks_required_modules: scripts/verify-odoo-dev.py references base, mail, sale, purchase, hr, account
  </behavior>
  <action>
Create `python/tests/test_dev_instance.py` with two test classes:

**Class TestDevInstanceConfig** -- Unit tests that validate the config files exist and contain expected content. These tests read files directly (no Docker needed). Use `pathlib.Path` to resolve PROJECT_ROOT (two parents up from tests/ dir, i.e., `Path(__file__).resolve().parents[2]`).

Tests:
- `test_compose_file_exists`: Assert docker/dev/docker-compose.yml exists.
- `test_compose_uses_named_volumes`: Read compose file, assert "odoo-dev-db" and "odoo-dev-data" appear. Assert "tmpfs" does NOT appear.
- `test_compose_separate_from_validation`: Assert dev compose contains `name: odoo-dev`. Assert validation compose (docker/docker-compose.yml) does NOT contain `name: odoo-dev`.
- `test_compose_healthcheck_uses_python`: Assert compose file contains `python3 -c` in healthcheck. Assert it does NOT use `curl` in healthcheck.
- `test_odoo_conf_settings`: Read docker/dev/odoo.conf. Assert contains `db_name = odoo_dev`, `list_db = False`, `admin_passwd = admin`.
- `test_env_file_defaults`: Read docker/dev/.env. Assert contains `ODOO_DEV_PORT=8069`.

**Class TestManagementScript** -- Unit tests that validate the management script content and permissions. No Docker needed.

Tests:
- `test_script_executable`: Assert scripts/odoo-dev.sh has executable bit set (os.access with os.X_OK).
- `test_script_uses_run_for_init`: Read script content. Assert `_init_modules` function body contains `run --rm` (not `exec`). This prevents the serialization failure documented in CLAUDE.md mistake #4.
- `test_script_has_all_commands`: Read script content. Assert all 5 commands appear: start, stop, status, reset, logs.
- `test_script_bash_syntax_valid`: Run `bash -n scripts/odoo-dev.sh` via subprocess and assert returncode == 0.
- `test_verify_script_parseable`: Read scripts/verify-odoo-dev.py. Use `ast.parse()` and assert no SyntaxError.
- `test_verify_script_checks_required_modules`: Read verify script content. Assert all 6 required module names appear: base, mail, sale, purchase, hr, account.

No pytest markers needed for this class -- these are pure unit tests that run without Docker.

Write tests FIRST (RED), then verify they pass against the files created in Plan 01 (GREEN).
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_dev_instance.py -x -q --timeout=30 -m "not docker"</automated>
  </verify>
  <done>
    - All unit tests in TestDevInstanceConfig and TestManagementScript pass
    - Tests validate file existence, content correctness, and script safety (run vs exec)
    - No Docker daemon required for these tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Docker integration tests for live instance verification</name>
  <files>python/tests/test_dev_instance.py</files>
  <behavior>
    - test_compose_starts_instance: docker compose up starts Odoo + PostgreSQL, healthcheck passes
    - test_xmlrpc_connectivity: XML-RPC authenticate succeeds, execute_kw on ir.model returns results
    - test_required_modules_installed: All 6 modules (base, mail, sale, purchase, hr, account) have state=installed
    - test_data_persistence: Create a partner record, stop/start instance, record still exists
  </behavior>
  <action>
Add a third test class to the EXISTING `python/tests/test_dev_instance.py` file:

**Class TestDevInstanceDocker** -- Integration tests requiring Docker. ALL tests in this class get both `@pytest.mark.docker` and `@skip_no_docker` decorators, following the pattern from `test_docker_integration.py`.

At the top of the file, add the skip decorator:
```python
from odoo_gen_utils.validation.docker_runner import check_docker_available
skip_no_docker = pytest.mark.skipif(
    not check_docker_available(), reason="Docker not available"
)
```

Add a module-level `pytestmark = pytest.mark.docker` ONLY IF all tests in the class use it. Since TestDevInstanceConfig and TestManagementScript do NOT need docker, use per-method decorators instead.

Use a class-scoped fixture `dev_instance` that:
1. Runs `scripts/odoo-dev.sh start` via subprocess (cwd=PROJECT_ROOT).
2. Waits up to 120 seconds for health (the script's start command handles waiting).
3. Yields the instance URL (http://localhost:{ODOO_DEV_PORT or 8069}).
4. In teardown: runs `scripts/odoo-dev.sh stop`.

NOTE: Do NOT run `reset` in teardown -- preserve data between test runs for faster iteration. Only `stop` the containers.

Tests:

- `test_compose_starts_instance(dev_instance)`: Assert the fixture URL is reachable. Use `urllib.request.urlopen(f"{url}/web/health")` and assert status 200.

- `test_xmlrpc_connectivity(dev_instance)`: Use `xmlrpc.client.ServerProxy` to authenticate as admin/admin on odoo_dev database. Assert uid is truthy. Call `execute_kw` on `ir.model` with `search_count` (empty domain). Assert count > 0.

- `test_required_modules_installed(dev_instance)`: Use XML-RPC to query `ir.module.module` with `[["state", "=", "installed"]]` domain, fields=["name"]. Extract names into a set. Assert {"base", "mail", "sale", "purchase", "hr", "account"} is a subset.

- `test_data_persistence(dev_instance)`:
  a. Create a res.partner record via `execute_kw` with name="odoo-gen-test-persistence".
  b. Record the created partner_id.
  c. Run `scripts/odoo-dev.sh stop` (subprocess).
  d. Run `scripts/odoo-dev.sh start` (subprocess).
  e. Wait for health.
  f. Re-authenticate via XML-RPC.
  g. Search for the partner by name. Assert it still exists.
  h. Clean up: unlink the test partner.

Each Docker test should have a generous timeout (use `@pytest.mark.timeout(180)` for persistence test, `@pytest.mark.timeout(120)` for others).

Write tests FIRST (RED), then confirm they would pass against a running instance (GREEN requires Docker -- verification is conditional).
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_dev_instance.py -x -q --timeout=30 -m "not docker" && echo "Unit tests pass (Docker tests skipped without Docker)"</automated>
  </verify>
  <done>
    - Docker integration tests exist in TestDevInstanceDocker class
    - All tests decorated with @pytest.mark.docker and @skip_no_docker
    - Tests cover: instance startup, XML-RPC connectivity, module installation, data persistence
    - When Docker is unavailable, tests are cleanly skipped (not failed)
    - Unit tests from Task 1 still pass
    - Full test suite: `uv run pytest tests/test_dev_instance.py --timeout=180` passes when Docker is available
  </done>
</task>

</tasks>

<verification>
- `cd python && uv run pytest tests/test_dev_instance.py -x -q --timeout=30 -m "not docker"` -- all unit tests pass
- `cd python && uv run pytest tests/ -x -q --timeout=30 -m "not docker and not e2e"` -- full unit suite passes (no regressions)
- When Docker available: `cd python && uv run pytest tests/test_dev_instance.py --timeout=180` -- all tests including Docker integration pass
</verification>

<success_criteria>
Unit tests validate all config files and scripts without Docker. Integration tests validate the full Odoo 17 CE dev instance lifecycle with Docker. All existing tests pass without regressions. Tests follow project conventions (docker marker, skip_no_docker decorator, pathlib for paths).
</success_criteria>

<output>
After completion, create `.planning/phases/15-odoo-dev-instance/15-02-SUMMARY.md`
</output>
