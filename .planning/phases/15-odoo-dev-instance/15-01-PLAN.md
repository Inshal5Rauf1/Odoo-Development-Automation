---
phase: 15-odoo-dev-instance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/dev/docker-compose.yml
  - docker/dev/odoo.conf
  - docker/dev/.env
  - scripts/odoo-dev.sh
  - scripts/verify-odoo-dev.py
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "docker compose -f docker/dev/docker-compose.yml config validates without errors"
    - "scripts/odoo-dev.sh is executable and accepts start|stop|status|reset|logs subcommands"
    - "scripts/verify-odoo-dev.py is a runnable Python script that performs XML-RPC auth + model query"
    - "Dev compose uses named volumes (odoo-dev-db, odoo-dev-data) for persistence"
    - "Dev compose is completely separate from existing ephemeral validation compose (docker/docker-compose.yml)"
  artifacts:
    - path: "docker/dev/docker-compose.yml"
      provides: "Persistent Odoo 17 CE + PostgreSQL 16 dev instance"
      contains: "odoo-dev-db"
    - path: "docker/dev/odoo.conf"
      provides: "Dev instance Odoo config with XML-RPC enabled and list_db=False"
      contains: "db_name = odoo_dev"
    - path: "docker/dev/.env"
      provides: "Default environment variables for dev instance"
      contains: "ODOO_DEV_PORT"
    - path: "scripts/odoo-dev.sh"
      provides: "Lifecycle management script for dev instance"
      min_lines: 50
    - path: "scripts/verify-odoo-dev.py"
      provides: "XML-RPC connectivity smoke test"
      min_lines: 30
  key_links:
    - from: "scripts/odoo-dev.sh"
      to: "docker/dev/docker-compose.yml"
      via: "COMPOSE_FILE variable pointing to docker/dev/docker-compose.yml"
      pattern: "docker compose -f.*docker/dev/docker-compose.yml"
    - from: "docker/dev/docker-compose.yml"
      to: "docker/dev/odoo.conf"
      via: "volume mount of odoo.conf into container"
      pattern: "odoo\\.conf:/etc/odoo/odoo\\.conf"
    - from: "scripts/verify-odoo-dev.py"
      to: "docker/dev/.env"
      via: "reads same default values (port 8069, db odoo_dev, user admin)"
      pattern: "localhost:8069"
---

<objective>
Create the persistent Docker Compose dev environment and management scripts for Odoo 17 CE.

Purpose: Provide a running Odoo 17 CE instance with PostgreSQL 16 that agents can connect to via XML-RPC (Phase 16 dependency). The dev instance is completely separate from the existing ephemeral validation compose to avoid port/volume conflicts.

Output: Docker Compose config, Odoo config, env defaults, management shell script, and XML-RPC smoke test script.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-odoo-dev-instance/15-RESEARCH.md

@docker/docker-compose.yml
@docker/odoo.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker Compose dev environment config</name>
  <files>docker/dev/docker-compose.yml, docker/dev/odoo.conf, docker/dev/.env</files>
  <action>
Create three files in docker/dev/:

1. **docker/dev/docker-compose.yml** -- Persistent Odoo 17 CE + PostgreSQL 16 dev instance.
   - Use `name: odoo-dev` to set the compose project name (prevents collision with validation compose).
   - PostgreSQL service (`db`): image `postgres:16`, env vars POSTGRES_USER=odoo, POSTGRES_PASSWORD=odoo, POSTGRES_DB=postgres, named volume `odoo-dev-db:/var/lib/postgresql/data`, healthcheck with `pg_isready -U odoo` (interval 5s, timeout 5s, retries 5), `restart: unless-stopped`.
   - Odoo service (`odoo`): image `odoo:17.0`, depends_on db with `condition: service_healthy`, port mapping `"${ODOO_DEV_PORT:-8069}:8069"`, env vars HOST=db USER=odoo PASSWORD=odoo, volumes for `odoo-dev-data:/var/lib/odoo` and `./odoo.conf:/etc/odoo/odoo.conf:ro`, healthcheck using python3 (NOT curl -- curl may not be in the image): `python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:8069/web/health')"` with interval 10s, timeout 5s, retries 12, start_period 30s, `restart: unless-stopped`.
   - Named volumes: `odoo-dev-db` and `odoo-dev-data` at top level.

2. **docker/dev/odoo.conf** -- Dev instance config.
   - `[options]` section with: addons_path=/mnt/extra-addons, data_dir=/var/lib/odoo, db_host=db, db_port=5432, db_user=odoo, db_password=odoo, admin_passwd=admin, without_demo=all, list_db=False, db_name=odoo_dev.
   - Comment that XML-RPC is enabled by default on port 8069.

3. **docker/dev/.env** -- Default environment variables.
   - ODOO_DEV_PORT=8069, ODOO_DEV_DB=odoo_dev, ODOO_DEV_USER=admin, ODOO_DEV_PASSWORD=admin.
   - Comment header explaining these are dev defaults only.

IMPORTANT: Keep the existing docker/docker-compose.yml and docker/odoo.conf UNTOUCHED. The dev compose is completely independent.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation && docker compose -f docker/dev/docker-compose.yml config --quiet 2>&1 && echo "Compose config valid"</automated>
  </verify>
  <done>
    - docker/dev/docker-compose.yml validates with `docker compose config`
    - Uses named volumes odoo-dev-db and odoo-dev-data (not tmpfs)
    - Uses python3 urllib for healthcheck (not curl)
    - Port configurable via ODOO_DEV_PORT env var
    - Compose project name set to odoo-dev
    - Existing docker/docker-compose.yml is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Create management script and XML-RPC smoke test</name>
  <files>scripts/odoo-dev.sh, scripts/verify-odoo-dev.py</files>
  <action>
Create scripts/ directory and two files:

1. **scripts/odoo-dev.sh** -- Lifecycle management script. Must be executable (chmod +x).

   Use `set -euo pipefail`. Resolve SCRIPT_DIR and PROJECT_ROOT. Set COMPOSE_FILE to `$PROJECT_ROOT/docker/dev/docker-compose.yml`. Read DB_NAME from `${ODOO_DEV_DB:-odoo_dev}`. Set MODULES to `base,mail,sale,purchase,hr,account`.

   Helper functions:
   - `_compose()`: runs `docker compose -f "$COMPOSE_FILE" "$@"`
   - `_db_exists()`: checks if database exists by running `psql -U odoo -tAc "SELECT 1 FROM pg_database WHERE datname='$DB_NAME'"` via `_compose exec -T db` and grepping for 1. Returns exit code.
   - `_init_modules()`: runs `_compose run --rm -T odoo odoo -d "$DB_NAME" -i "$MODULES" --stop-after-init --no-http --log-level=warn`. MUST use `run --rm` NOT `exec` (exec causes serialization failures -- see CLAUDE.md mistake #4). Prints progress messages before and after.
   - `_wait_healthy()`: loops up to 60 seconds checking `_compose ps --format json` for odoo service health status. Prints "Waiting for Odoo to be healthy..." and dots. Exits with error if timeout.

   Commands (via case statement on `${1:-help}`):
   - `start`: Print starting message. Run `_compose up -d --wait db`. Check `_db_exists`; if false, call `_init_modules`. Then `_compose up -d --wait`. Print ready message with URL `http://localhost:${ODOO_DEV_PORT:-8069}`.
   - `stop`: Run `_compose down`. Print stopped message.
   - `status`: Run `_compose ps`. Then attempt to run `python3 "$SCRIPT_DIR/verify-odoo-dev.py"` if it exists, suppressing errors.
   - `reset`: Print warning. Run `_compose down -v`. Print message that data is destroyed and user should run start to reinitialize.
   - `logs`: Run `_compose logs -f "${2:-odoo}"`.
   - `*` (help): Print `Usage: $0 {start|stop|status|reset|logs}`.

2. **scripts/verify-odoo-dev.py** -- XML-RPC connectivity smoke test. Standalone Python script (no project dependencies, stdlib only).

   Function `verify_xmlrpc(url, db, username, password)` with defaults from env vars: url from `ODOO_URL` or `http://localhost:{ODOO_DEV_PORT or 8069}`, db from `ODOO_DEV_DB` or `odoo_dev`, username from `ODOO_DEV_USER` or `admin`, password from `ODOO_DEV_PASSWORD` or `admin`.

   Steps:
   a. Create `xmlrpc.client.ServerProxy(f"{url}/xmlrpc/2/common")` and call `version()`. Print server version.
   b. Call `authenticate(db, username, password, {})`. Assert uid is truthy. Print uid.
   c. Create `xmlrpc.client.ServerProxy(f"{url}/xmlrpc/2/object")`. Call `execute_kw` to `search_count` on `ir.model` (empty domain). Print model count.
   d. Call `execute_kw` to `search_read` on `ir.module.module` with domain `[["state", "=", "installed"]]` and fields `["name"]`. Check that {base, mail, sale, purchase, hr, account} are all present. Print installed count and any missing modules.
   e. Return True if all checks pass, False otherwise.

   Error handling: catch `ConnectionRefusedError` (print "Connection refused, is Odoo running?"), `xmlrpc.client.Fault` (print fault string), and generic `Exception` (print error).

   `if __name__ == "__main__"`: call `verify_xmlrpc()`, `sys.exit(0 if success else 1)`.

   Use os.environ.get() for all env var reads. Add a shebang `#!/usr/bin/env python3`.

After creating both files, run `chmod +x scripts/odoo-dev.sh scripts/verify-odoo-dev.py`.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation && bash -n scripts/odoo-dev.sh && python3 -c "import ast; ast.parse(open('scripts/verify-odoo-dev.py').read())" && echo "Both scripts parse OK"</automated>
  </verify>
  <done>
    - scripts/odoo-dev.sh is executable, passes bash syntax check
    - scripts/odoo-dev.sh accepts start|stop|status|reset|logs subcommands
    - scripts/odoo-dev.sh uses `run --rm` for init (NOT exec)
    - scripts/odoo-dev.sh resolves PROJECT_ROOT and COMPOSE_FILE correctly
    - scripts/verify-odoo-dev.py parses as valid Python
    - scripts/verify-odoo-dev.py reads connection params from env vars with sensible defaults
    - scripts/verify-odoo-dev.py checks authentication, model count, and required module installation
  </done>
</task>

</tasks>

<verification>
- `docker compose -f docker/dev/docker-compose.yml config` exits 0
- `bash -n scripts/odoo-dev.sh` exits 0
- `python3 -c "import ast; ast.parse(open('scripts/verify-odoo-dev.py').read())"` exits 0
- Existing docker/docker-compose.yml is unchanged (diff shows no changes)
- docker/dev/ directory contains docker-compose.yml, odoo.conf, .env
- scripts/ directory contains odoo-dev.sh (executable), verify-odoo-dev.py (executable)
</verification>

<success_criteria>
All Docker config files and scripts created, validated syntactically, and the existing validation compose is untouched. No Docker daemon needed for this plan's verification -- syntax/parse checks only.
</success_criteria>

<output>
After completion, create `.planning/phases/15-odoo-dev-instance/15-01-SUMMARY.md`
</output>
