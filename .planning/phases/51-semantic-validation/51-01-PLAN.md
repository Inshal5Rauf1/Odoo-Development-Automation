---
phase: 51-semantic-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/src/odoo_gen_utils/validation/semantic.py
  - python/tests/test_semantic_validation.py
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "ast.parse() catches Python syntax errors in generated .py files (E1)"
    - "xml.etree.parse() catches malformed XML in generated .xml files (E2)"
    - "View field references that don't exist on the model are reported as errors with fuzzy suggestions (E3)"
    - "ACL CSV entries referencing non-existent model XML IDs are reported as errors (E4)"
    - "Duplicate XML IDs across data files are reported as errors (E5)"
    - "Missing manifest depends for cross-module imports are reported as errors (E6)"
    - "Comodel references are checked against registry and known models as warnings (W1)"
    - "Computed field @api.depends references are validated as warnings (W2)"
    - "Security group references in views are validated as warnings (W3)"
    - "Record rule domain field references are validated as warnings (W4)"
    - "Short-circuit skips cross-ref checks when E1/E2 fails for a file"
    - "Full validation completes in under 2 seconds for a typical module"
  artifacts:
    - path: "python/src/odoo_gen_utils/validation/semantic.py"
      provides: "All 10 validation checks (E1-E6, W1-W4), ValidationIssue, SemanticValidationResult, semantic_validate(), print_validation_report()"
      contains: "class ValidationIssue"
    - path: "python/tests/test_semantic_validation.py"
      provides: "Unit tests for all 10 checks, short-circuit, performance budget"
      min_lines: 200
  key_links:
    - from: "python/src/odoo_gen_utils/validation/semantic.py"
      to: "python/src/odoo_gen_utils/registry.py"
      via: "ModelRegistry passed as optional param to semantic_validate()"
      pattern: "registry.*ModelRegistry.*None"
    - from: "python/src/odoo_gen_utils/validation/semantic.py"
      to: "python/src/odoo_gen_utils/data/known_odoo_models.json"
      via: "JSON load for comodel validation fallback"
      pattern: "known_odoo_models"
---

<objective>
Create the semantic validation module with all 10 checks (E1-E6 errors, W1-W4 warnings) and comprehensive TDD tests.

Purpose: Catch 60-70% of generation bugs in <1 second without Docker -- field reference errors, XML ID conflicts, ACL mismatches, manifest gaps.
Output: `validation/semantic.py` with `semantic_validate()` entry point + full test suite.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-semantic-validation/51-CONTEXT.md
@.planning/phases/51-semantic-validation/51-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From python/src/odoo_gen_utils/validation/types.py:
```python
@dataclass(frozen=True)
class Violation:
    file: str
    line: int
    column: int
    rule_code: str
    symbol: str
    severity: str
    message: str
    suggestion: str | None = None
```

From python/src/odoo_gen_utils/registry.py:
```python
class ModelRegistry:
    def __init__(self, registry_path: Path) -> None: ...
    def load(self) -> None: ...
    def load_known_models(self) -> None: ...
    def _is_known_model(self, model_name: str, spec: dict[str, Any]) -> bool: ...
    # models: dict[str, dict] — model_name -> model_data
    # known_models: dict[str, Any] — from known_odoo_models.json
```

From python/src/odoo_gen_utils/validation/__init__.py:
```python
# Re-exports from submodules — semantic.py exports should be added here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create semantic validation module with all 10 checks (TDD)</name>
  <files>python/src/odoo_gen_utils/validation/semantic.py, python/tests/test_semantic_validation.py</files>
  <behavior>
    - E1: ast.parse() valid Python returns no issues; SyntaxError returns error with file + line
    - E2: xml.etree.parse() valid XML returns no issues; malformed XML returns error with file
    - E3: Field ref in view arch that exists on model returns no issue; missing field returns error with difflib suggestion (cutoff=0.6)
    - E3: Fields from _inherit (e.g., mail.thread -> message_ids) are recognized as valid
    - E3: Top-level view metadata fields (name, model, arch, priority, inherit_id) are NOT checked as model fields
    - E4: ACL CSV with valid model_id:id returns no issue; unknown model ref returns error with line number
    - E4: group_id:id with module prefix (e.g., base.group_user) is handled correctly
    - E5: Unique XML IDs across files returns no issue; duplicate ID returns error listing both files
    - E6: Manifest depends includes all modules referenced by imports returns no issue; missing dep returns error
    - E6: XML ref="" attributes referencing external modules are checked for depends
    - W1: comodel_name in registry or known_odoo_models.json returns no issue; unknown returns warning
    - W2: @api.depends('field_name') where field exists returns no issue; missing returns warning
    - W2: Dot-notation depends ('student_id.name') only validates first segment
    - W3: groups="module.group_name" in XML views where group exists returns no issue; unknown returns warning
    - W4: ir.rule domain_force field references that exist on model return no issue; missing returns warning
    - Short-circuit: E1 failure on a .py file skips E3/W1/W2 for that file; E2 failure on .xml skips E3/E5/W3/W4
    - Performance: 10-model module validates in under 2 seconds
    - semantic_validate(output_dir, registry=None) returns SemanticValidationResult
    - print_validation_report(result) prints human-friendly output
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    Create `python/tests/test_semantic_validation.py` with test fixtures using `tmp_path`:
    - Build valid and invalid module directory structures in each test (or shared fixtures)
    - Valid module: __manifest__.py, models/model.py with fields, views/views.xml with matching field refs, security/ir.model.access.csv with valid refs
    - Invalid module: syntax-error .py, malformed .xml, view refs to non-existent fields, ACL refs to wrong models, duplicate XML IDs, missing manifest depends

    Test classes: TestE1PythonSyntax, TestE2XmlWellFormedness, TestE3FieldReferences, TestE4AclReferences, TestE5XmlIdUniqueness, TestE6ManifestDepends, TestW1Comodel, TestW2ComputedDepends, TestW3GroupRefs, TestW4RuleDomain, TestShortCircuit, TestPerformance

    Then create `python/src/odoo_gen_utils/validation/semantic.py`:
    - Frozen dataclasses: `ValidationIssue(code, severity, file, line, message, fixable, suggestion)` and `SemanticValidationResult(module, errors, warnings, duration_ms)` with `has_errors` and `has_fixable_errors` as @property
    - Internal index types: `ParsedModel(model_name, fields, comodels, inherits, imports, depends_decorators, file_path, line_numbers)` and `ParsedXml(record_ids, field_refs, group_refs, external_refs, file_path)`
    - Pure function `semantic_validate(output_dir: Path, registry: ModelRegistry | None = None) -> SemanticValidationResult`
    - Execution order: E1 -> E2 -> E5 -> E3 -> E4 -> E6 -> W1 -> W2 -> W3 -> W4
    - Short-circuit: track `failed_py` and `failed_xml` sets, skip cross-ref checks for those files
    - Single-pass parsing: parse each .py once for AST, each .xml once for ET, build in-memory indexes
    - Use `difflib.get_close_matches(cutoff=0.6)` for E3 field name suggestions
    - For E3, only check fields INSIDE arch content (form/tree/search elements), skip view metadata fields
    - For E3, include inherited fields from _inherit list via known_odoo_models.json lookup
    - For E4, split group_id:id on '.' for module-prefixed references
    - For E6, parse manifest with ast.literal_eval, check imports AND XML ref="" attributes
    - For W2, only validate first segment of dot-notation depends
    - `print_validation_report(result)` with error/warning symbols and file:line references
    - Use `time.perf_counter()` for duration measurement
    - All stdlib: ast, xml.etree.ElementTree, csv, difflib, dataclasses, time, pathlib

    Add exports to `python/src/odoo_gen_utils/validation/__init__.py`: ValidationIssue, SemanticValidationResult, semantic_validate, print_validation_report
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && python -m pytest tests/test_semantic_validation.py -x -q --tb=short</automated>
  </verify>
  <done>All 10 checks (E1-E6, W1-W4) implemented and tested. Short-circuit behavior verified. Performance under 2s confirmed. semantic_validate() returns structured SemanticValidationResult with errors/warnings/duration_ms.</done>
</task>

</tasks>

<verification>
- `cd /home/inshal-rauf/Odoo_module_automation/python && python -m pytest tests/test_semantic_validation.py -x -q --tb=short` -- all semantic validation tests pass
- `cd /home/inshal-rauf/Odoo_module_automation/python && python -m pytest --tb=short -q` -- full suite regression passes
</verification>

<success_criteria>
- semantic_validate() catches field ref errors, XML ID duplicates, ACL mismatches, manifest gaps
- All 10 checks work with correct error/warning severity
- Short-circuit prevents cascading false positives
- Validation under 2s for typical module
- Zero external dependencies (all stdlib)
</success_criteria>

<output>
After completion, create `.planning/phases/51-semantic-validation/51-01-SUMMARY.md`
</output>
