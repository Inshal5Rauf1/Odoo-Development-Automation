---
phase: 08-search-fork-extend
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - python/pyproject.toml
  - python/src/odoo_gen_utils/search/__init__.py
  - python/src/odoo_gen_utils/search/index.py
  - python/src/odoo_gen_utils/search/types.py
  - python/src/odoo_gen_utils/cli.py
  - python/tests/test_search_index.py
  - python/tests/test_cli_build_index.py
  - commands/index.md
autonomous: true
requirements: [FORK-04, SRCH-01, SRCH-02]

must_haves:
  truths:
    - "odoo-gen-utils build-index crawls OCA repos via PyGithub and stores module metadata in ChromaDB"
    - "odoo-gen-utils index-status reports index age, module count, and storage location"
    - "Index build fails with clear auth error when no GITHUB_TOKEN or gh auth token available"
    - "search optional-dependencies install chromadb, sentence-transformers, torch (CPU-only), PyGithub, gitpython"
    - "Index entries contain module_name, summary, category, depends, oca_repo, github_url metadata"
  artifacts:
    - path: "python/pyproject.toml"
      provides: "[search] optional-dependencies + CPU torch index config"
      contains: "chromadb"
    - path: "python/src/odoo_gen_utils/search/__init__.py"
      provides: "Public API exports for search package"
      exports: ["build_oca_index", "get_index_status", "get_github_token"]
    - path: "python/src/odoo_gen_utils/search/index.py"
      provides: "OCA crawl, manifest parse, ChromaDB upsert pipeline"
      min_lines: 80
    - path: "python/src/odoo_gen_utils/search/types.py"
      provides: "Frozen dataclasses: IndexEntry, IndexStatus"
      min_lines: 20
    - path: "python/src/odoo_gen_utils/cli.py"
      provides: "build-index and index-status Click commands"
      contains: "build-index"
    - path: "python/tests/test_search_index.py"
      provides: "Tests for index build, entry structure, auth error handling"
      min_lines: 60
    - path: "python/tests/test_cli_build_index.py"
      provides: "Tests for build-index and index-status CLI commands"
      min_lines: 30
    - path: "commands/index.md"
      provides: "GSD command stub for /odoo-gen:index"
  key_links:
    - from: "python/src/odoo_gen_utils/cli.py"
      to: "python/src/odoo_gen_utils/search/__init__.py"
      via: "import build_oca_index, get_index_status, get_github_token from public API"
      pattern: "from odoo_gen_utils\\.search import"
    - from: "python/src/odoo_gen_utils/cli.py"
      to: "python/src/odoo_gen_utils/search/index.py"
      via: "import DEFAULT_DB_PATH"
      pattern: "from odoo_gen_utils\\.search\\.index import DEFAULT_DB_PATH"
    - from: "python/src/odoo_gen_utils/search/index.py"
      to: "chromadb.PersistentClient"
      via: "ChromaDB collection upsert"
      pattern: "chromadb\\.PersistentClient"
    - from: "python/src/odoo_gen_utils/search/index.py"
      to: "github.Github"
      via: "PyGithub OCA org crawl"
      pattern: "Github\\(token\\)"
---

<objective>
Build the vector index infrastructure: ChromaDB collection, OCA repo crawl via PyGithub, manifest parsing, and CLI commands (build-index, index-status). Also configure search optional-dependencies in pyproject.toml with CPU-only torch.

Purpose: FORK-04 requires a local vector index. SRCH-01/02 depend on indexed module data to search against. This is the foundation all search and fork operations require.
Output: Working build-index and index-status CLI commands, ChromaDB storage at ~/.local/share/odoo-gen/chromadb/, pyproject.toml with [search] extras.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-search-fork-extend/08-CONTEXT.md
@.planning/phases/08-search-fork-extend/08-RESEARCH.md

@python/pyproject.toml
@python/src/odoo_gen_utils/cli.py
@python/src/odoo_gen_utils/search/__init__.py

<interfaces>
<!-- Existing CLI pattern from cli.py — new commands follow the same Click group pattern -->
From python/src/odoo_gen_utils/cli.py:
```python
@click.group()
@click.version_option(version=__version__)
def main() -> None:
    """odoo-gen-utils: Python utilities for the odoo-gen GSD extension."""

# Existing commands: render, list-templates, render-module, validate-kb, extract-i18n, validate
# New commands to add: build-index, index-status
```

From python/src/odoo_gen_utils/__init__.py:
```python
__version__ = "0.1.0"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, index module, and TDD test scaffold</name>
  <files>
    python/pyproject.toml,
    python/src/odoo_gen_utils/search/__init__.py,
    python/src/odoo_gen_utils/search/types.py,
    python/src/odoo_gen_utils/search/index.py,
    python/tests/test_search_index.py
  </files>
  <behavior>
    - Test: build_oca_index() with mocked PyGithub returns correct count of indexed modules
    - Test: build_oca_index() skips repos without a 17.0 branch (mock get_branch raising GithubException)
    - Test: build_oca_index() skips modules where installable=False in manifest
    - Test: build_oca_index() raises SystemExit when no token provided (auth detection)
    - Test: _parse_manifest_safe() returns dict for valid manifest content
    - Test: _parse_manifest_safe() returns None for invalid/malicious content (never uses eval())
    - Test: _build_document_text() concatenates name + summary + description + category + depends
    - Test: get_index_status() returns IndexStatus with module_count=0 when no index exists
    - Test: get_index_status() returns IndexStatus with correct count and timestamp for existing index
    - Test: IndexEntry frozen dataclass has all required metadata fields
  </behavior>
  <action>
    **RED phase first.** Create test file with all failing tests before writing implementation.

    1. Update python/pyproject.toml:
       - Add `[project.optional-dependencies] search = ["chromadb>=1.5", "sentence-transformers>=5.2", "torch>=2.6", "PyGithub>=2.8", "gitpython>=3.1"]`
       - Add `[[tool.uv.index]]` section with name="pytorch-cpu", url="https://download.pytorch.org/whl/cpu", explicit=true
       - Add `[tool.uv.sources]` section with `torch = [{ index = "pytorch-cpu" }]`

    2. Create python/src/odoo_gen_utils/search/types.py:
       - `IndexEntry` frozen dataclass: module_name, display_name, summary, description, depends (tuple[str,...]), category, oca_repo, github_url, stars (int), last_pushed (str)
       - `IndexStatus` frozen dataclass: exists (bool), module_count (int), last_built (str|None), db_path (str), size_bytes (int)

    3. Create python/src/odoo_gen_utils/search/__init__.py:
       - Export: build_oca_index, get_index_status, get_github_token, IndexEntry, IndexStatus

    4. Create python/src/odoo_gen_utils/search/index.py:
       - `DEFAULT_DB_PATH = Path.home() / ".local" / "share" / "odoo-gen" / "chromadb"`
       - `get_github_token() -> str | None`: check GITHUB_TOKEN env, then try `gh auth token` via subprocess, return None if both fail. NOTE: This is a PUBLIC function (no underscore prefix) — exported from search/__init__.py for use by cli.py and other consumers.
       - `_parse_manifest_safe(content: str) -> dict | None`: use ast.literal_eval, NEVER eval(). Return None on ValueError/SyntaxError. Validate result is dict.
       - `_build_document_text(manifest: dict, module_name: str) -> str`: concatenate display_name + summary + description (first 500 chars) + category + depends. Join with " | ".
       - `build_oca_index(token: str, db_path: str, incremental: bool = False, progress_callback: Callable | None = None) -> int`:
         - Create chromadb.PersistentClient(path=db_path)
         - collection = client.get_or_create_collection(name="odoo_modules", metadata={"hnsw:space": "cosine"})
         - Store build timestamp in collection metadata
         - Iterate OCA org repos via PyGithub: `Github(token).get_organization("OCA").get_repos()`
         - For each repo: try get_branch("17.0"), skip on failure
         - For each top-level dir with __manifest__.py: parse manifest, skip if not installable
         - Upsert to ChromaDB with id="oca/{repo.name}/{module_name}", document=_build_document_text(...), metadatas={module, repo, org, category, depends, version, license, summary, url, stars, last_pushed}
         - If incremental: skip repos where pushed_at <= stored last_pushed
         - Call progress_callback(repos_done, total_repos) if provided
         - Return count of modules indexed
       - `get_index_status(db_path: str | None = None) -> IndexStatus`: check if ChromaDB dir exists, count entries, read build timestamp from collection metadata

    Tests must mock PyGithub (Github, Organization, Repository, ContentFile) and chromadb.PersistentClient. Use unittest.mock.patch for external dependencies. Do NOT require actual GitHub auth or ChromaDB installation for tests to pass.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_search_index.py -x -v</automated>
  </verify>
  <done>All 10+ tests pass. IndexEntry and IndexStatus types are frozen dataclasses. build_oca_index crawls mocked OCA repos and upserts to mocked ChromaDB. Auth detection returns clear error. Manifest parsing is safe (ast.literal_eval only). get_github_token is a public function (no underscore).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI commands (build-index, index-status) and index.md command stub</name>
  <files>
    python/src/odoo_gen_utils/cli.py,
    python/tests/test_cli_build_index.py,
    commands/index.md
  </files>
  <behavior>
    - Test: build-index with no token available (mock get_github_token returning None) exits code 1 with auth instructions message
    - Test: build-index with token calls build_oca_index and prints success message with module count
    - Test: index-status --json outputs valid JSON containing "exists", "module_count", "db_path" keys
    - Test: index-status (no --json) prints human-readable status with module count and db path
  </behavior>
  <action>
    **RED phase first.** Write test_cli_build_index.py with all failing tests before implementation.

    Tests use Click's CliRunner to invoke commands. Mock get_github_token, build_oca_index, and get_index_status from odoo_gen_utils.search.

    1. Create python/tests/test_cli_build_index.py:
       - Use `from click.testing import CliRunner` and `from odoo_gen_utils.cli import main`
       - Mock `odoo_gen_utils.search.get_github_token` (public API, not private _get_github_token)
       - Mock `odoo_gen_utils.search.build_oca_index` to return an int count
       - Mock `odoo_gen_utils.search.get_index_status` to return an IndexStatus

    2. Add to python/src/odoo_gen_utils/cli.py — two new Click commands following existing patterns:

    **build-index command:**
    ```python
    @main.command("build-index")
    @click.option("--token", envvar="GITHUB_TOKEN", default=None, help="GitHub personal access token")
    @click.option("--db-path", default=None, help="ChromaDB storage path (default: ~/.local/share/odoo-gen/chromadb/)")
    @click.option("--update", is_flag=True, help="Only re-index repos pushed since last build")
    def build_index(token: str | None, db_path: str | None, update: bool) -> None:
    ```
    - If token is None: try `get_github_token()` from search package (public function, no underscore)
    - If still None: display clear auth error per Decision D (CONTEXT.md):
      ```
      Index build requires GitHub authentication.
      Run: gh auth login
      Or set: export GITHUB_TOKEN=your_token
      Then re-run your search.
      ```
      Exit with code 1.
    - Resolve db_path to DEFAULT_DB_PATH if None
    - Show progress: "Building OCA module index..." with callback printing "Indexing OCA repos... {done}/{total}"
    - Call build_oca_index(token, resolved_path, incremental=update, progress_callback=...)
    - On success: "Indexed {count} modules from OCA"

    **index-status command:**
    ```python
    @main.command("index-status")
    @click.option("--db-path", default=None, help="ChromaDB storage path")
    @click.option("--json", "json_output", is_flag=True, help="Output as JSON")
    def index_status(db_path: str | None, json_output: bool) -> None:
    ```
    - Call get_index_status(db_path)
    - If json_output: print JSON
    - Else: print human-readable status (exists, module count, last built, db path)

    Add imports at top of cli.py:
    ```python
    from odoo_gen_utils.search import build_oca_index, get_index_status, get_github_token
    from odoo_gen_utils.search.index import DEFAULT_DB_PATH
    ```

    3. Create commands/index.md — GSD command registration for `/odoo-gen:index`:
    ```markdown
    ---
    name: odoo-gen:index
    description: Build or update the local ChromaDB index of OCA Odoo modules for semantic search
    argument-hint: "[--update]"
    ---
    <objective>
    Build or refresh the local vector index of OCA Odoo modules. The index powers `/odoo-gen:search` for fast semantic matching.

    On first run, crawls all OCA GitHub repositories with a 17.0 branch, extracts module metadata from __manifest__.py files, and stores embeddings in a local ChromaDB database.

    Use `--update` flag to incrementally re-index only repos that changed since the last build.
    </objective>

    <workflow>
    1. Verify GitHub authentication (GITHUB_TOKEN env var or gh auth token)
    2. Crawl OCA organization repos via GitHub API
    3. Extract __manifest__.py metadata from each module with a 17.0 branch
    4. Embed and store module descriptions in ChromaDB at ~/.local/share/odoo-gen/chromadb/
    5. Report indexed module count and storage location
    </workflow>
    ```
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_cli_build_index.py tests/test_search_index.py tests/test_cli_validate.py -x -q && .venv/bin/python -c "from odoo_gen_utils.cli import main; print('CLI imports OK')"</automated>
  </verify>
  <done>build-index and index-status commands are registered in cli.py. Auth error message matches CONTEXT.md Decision D exactly. commands/index.md stub exists for GSD registration. CLI imports use public get_github_token (no underscore). All 4 CLI tests pass. CLI imports succeed without search dependencies (lazy import or graceful error).</done>
</task>

</tasks>

<verification>
1. `cd python && .venv/bin/python -m pytest tests/test_search_index.py -x -v` -- all tests pass
2. `cd python && .venv/bin/python -m pytest tests/test_cli_build_index.py -x -v` -- all CLI command tests pass
3. `cd python && .venv/bin/python -c "from odoo_gen_utils.search.types import IndexEntry, IndexStatus; print('Types OK')"` -- types importable
4. `cd python && .venv/bin/python -c "from odoo_gen_utils.cli import main; print('CLI OK')"` -- CLI loads without crash
5. `grep 'chromadb' python/pyproject.toml` -- search dependencies present
6. `test -f commands/index.md` -- command stub exists
7. `cd python && .venv/bin/python -c "from odoo_gen_utils.search import get_github_token; print('Public API OK')"` -- get_github_token is public
</verification>

<success_criteria>
- pyproject.toml has [search] optional-dependencies with chromadb, sentence-transformers, torch, PyGithub, gitpython
- pyproject.toml has CPU-only torch index configuration
- search/ package exists with types.py, index.py, __init__.py
- build_oca_index() crawls mocked OCA repos and returns module count
- get_index_status() reports index metadata
- get_github_token() is a public function exported from search/__init__.py (no underscore prefix)
- Auth detection follows Decision D pattern exactly
- Manifest parsing uses ast.literal_eval (NEVER eval)
- build-index and index-status CLI commands work
- CLI commands have TDD tests in test_cli_build_index.py
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-fork-extend/08-01-SUMMARY.md`
</output>
